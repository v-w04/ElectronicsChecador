/**
 * ============================================================================
 *  Código.gs — VACÍO A PROPÓSITO
 * ============================================================================
 *
 *  Este archivo tenía las 2,506 líneas del backend completo. Se partió en
 *  módulos para poder trabajar en uno sin arriesgar el resto:
 *
 *    Config.gs         constantes, turnos, días laborables, normalizador de ID
 *    WebAPI.gs         doGet / doPost y la lista de funciones expuestas
 *    Usuarios.gs       usuarios, PINs, contraseñas, avatares
 *    Checadas.gs       guardar checadas, detectar tipo, veredicto
 *    Perfil.gs         perfil del empleado, quincena, dispositivos
 *    Push.gs           Firebase, tokens, envío y bitácora PUSH_LOG
 *    Alertas.gs        el motor que corre cada minuto
 *    Mensajes.gs       los textos de las alertas
 *    Ausencias.gs      ausencias y excepciones del día
 *    Juegos.gs         tablero de juegos
 *    Mantenimiento.gs  diagnóstico y limpieza
 *
 *  No se borró desde aquí porque clasp no borra archivos del proyecto en
 *  línea: puedes eliminarlo en el editor de Apps Script cuando quieras.
 *  Dejarlo tampoco estorba.
 * ============================================================================
 */
